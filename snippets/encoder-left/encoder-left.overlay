// Left peripheral encoder overlay.
// J101逆向き接続: A=GPIO0.20(CS), B=GPIO0.19(MOTION)
// SW診断: GPIO0.20をOUTPUTに使わず、a=GPIO0.20を復元してエンコーダー安定確認。
// SWはkscan-gpio-directのみ(GPIO0.18, ACTIVE_HIGH|PULL_DOWN)で暫定。
// SPI0を無効化してGPIO0.18/0.20を解放する。

&spi0 {
    status = "disabled";
};

&i2c0 {
    status = "disabled";
};

&encoder {
    a-gpios = <&gpio0 20 (GPIO_ACTIVE_HIGH | GPIO_PULL_UP)>;
    b-gpios = <&gpio0 19 (GPIO_ACTIVE_HIGH | GPIO_PULL_UP)>;
    status = "okay";
};

/ {
    chosen {
        zmk,kscan = <&kscan_enc_composite>;
    };

    kscan_enc_sw: kscan_enc_sw {
        compatible = "zmk,kscan-gpio-direct";
        input-gpios = <&gpio0 18 (GPIO_ACTIVE_HIGH | GPIO_PULL_DOWN)>;
        wakeup-source;
    };

    kscan_enc_composite: kscan_enc_composite {
        compatible = "zmk,kscan-composite";
        rows = <5>;
        columns = <12>;

        matrix {
            kscan = <&kscan0>;
        };

        enc_sw {
            kscan = <&kscan_enc_sw>;
            row-offset = <4>;
            col-offset = <10>;
        };
    };
};

&physical_layout_s { kscan = <&kscan_enc_composite>; };
&physical_layout_m { kscan = <&kscan_enc_composite>; };
&physical_layout_l { kscan = <&kscan_enc_composite>; };
