# 左ペリフェラル専用。encoder スニペットと併せて指定する。

# sensor-bindings の解決はセントラルでのみ行われる (ZMK はペリフェラルで
# keymap.c とビヘイビアをコンパイルしない)。さらに *_STUDIO_RPC は
# ZMK_STUDIO_RPC を持たないペリフェラルではビルドできないため、
# ペリフェラル側では runtime sensor rotate ごと無効にする。
# CONFIG_ZMK_RUNTIME_SENSOR_ROTATE is not set
