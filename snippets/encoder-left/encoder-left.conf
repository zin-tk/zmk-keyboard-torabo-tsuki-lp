# BLE TX buffers
CONFIG_BT_CONN_TX_MAX=10
CONFIG_BT_BUF_ACL_TX_COUNT=10

# sensor_state_msgq and position_state_msgq share this queue size (default 10).
# Encoder rapid rotation fills the sensor queue, stalling all kscan event processing.
CONFIG_ZMK_SPLIT_BLE_PERIPHERAL_POSITION_QUEUE_SIZE=50
